import { ReadexcelDirective } from './readexcel.directive';

describe('ReadexcelDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadexcelDirective();
    expect(directive).toBeTruthy();
  });
});
